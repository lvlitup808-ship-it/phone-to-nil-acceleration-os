name: Golden nightly
on:
  schedule:
    - cron: "20 6 * * *"
  workflow_dispatch:
jobs:
  harness:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python -m pip install -e ".[dev]"
      - run: python -m pytest tests/golden/test_pose_pipeline.py -q
