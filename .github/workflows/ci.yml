name: CI
on:
  push:
    branches: [main, feat/**]
  pull_request:
jobs:
  python:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python -m pip install -e ".[dev]"
      - run: python -m ruff check services packages tests
      - run: python -m mypy services packages
      - run: python -m compileall -q services packages tests
      - run: python -m packages.shared.schemas
      - run: python -m pytest tests -q
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build -f infra/docker/api.Dockerfile -t accel-api .
