# Notes

Phone-to-NIL Acceleration OS is not a 40-time app.

Primary KPIs: shin angle, hip height, ground-contact time, first-step separation, asymmetry.

Jev owns bounded pipeline decisions. LLMs own cue language and NIL narrative — only after evidence survives the RAG filter.

Fallback: if `TYPESAFE_API_KEY` is missing, `packages/judgment` uses deterministic heuristics so CI and local MVP still run.
